# Internal working material (round plans, ledgers, reviews, reproductions) stays
# on disk for the local loop but is never tracked. Only the documents published
# with the package are versioned; add a negation here to publish a new one.
*
!.gitignore
!design.md
!known-limitations.md
!validation.md
